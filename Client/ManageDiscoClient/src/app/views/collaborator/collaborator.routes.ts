import { RouterModule } from "@angular/router";
import { CollaboratorsComponent } from "./collaborator.component";

export const CollaboratorRoute = RouterModule.forChild([
  { path: '', component: CollaboratorsComponent }  

])
