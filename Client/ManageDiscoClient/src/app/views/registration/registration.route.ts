import { RouterModule } from "@angular/router";
import { RegistrationComponent } from "./registration.component";

export const RegistrationRoutes = RouterModule.forChild([
  {path: '', component: RegistrationComponent}
])
